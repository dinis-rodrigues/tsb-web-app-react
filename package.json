{
  "name": "@tsb/root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "all": "lerna run start",
    "web": "lerna run start --scope=@tsb/frontend",
    "server": "lerna run start --scope=backend"
  },
  "devDependencies": {}
}
